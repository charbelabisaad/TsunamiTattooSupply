<div class="modal fade" id="deleteRoleModal" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog modal-md" role="document">
        <div class="modal-content">

            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title">Delete Role</h5>
                <button type="button" class="close text-white" data-bs-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>

            <form method="post">
                <div class="modal-body">
                    <p class="mb-3" id="deleteRoleMessage">
                        Are you sure you want to delete this roleid?
                    </p>
                    <input type="hidden" id="deletedRoleId" value="0" />
                </div>

                <div class="modal-footer">
                    <div class="row w-100">
                        <div id="deleteModalMessage" class="col-md-6 d-flex align-items-center text-danger" style="display:none;"></div>

                        <div class="col-md-6 d-flex align-items-center justify-content-end">
                            <button type="button" class="btn btn-secondary me-2" style="width:100px !important" data-bs-dismiss="modal">
                                <i class="fas fa-ban"></i> Cancel
                            </button>
                            <button id="btnConfirmDeleteRole" type="button" class="btn btn-danger ml-1" style="width:100px!important">
                                <span class="spinner-border spinner-border-sm d-none" id="btnDeleteSpinner" role="status" aria-hidden="true"></span>
                                <i class="fas fa-trash"></i> Delete
                            </button>
                        </div>
                    </div>
                </div>
            </form>

        </div>
    </div>
</div>



<script>

    document.addEventListener("DOMContentLoaded", function () {
         
        $('#btnConfirmDeleteRole').on('click', function (e) {
            e.preventDefault();
             
            const roleId = $("#deletedRoleId").val();

            $("#btnDeleteSpinner").removeClass("d-none");
            $("#btnConfirmDeleteRole i").addClass("d-none");
            $("#btnConfirmDeleteRole").prop("disabled", true)
            //$("#btnConfirmDeleteRole").addClass("disabled")

            $.ajax({
                url: '@Url.Action("DeleteRole", "Roles")',
                type: "POST",
                data: { ID: roleId }
            })
            .done(function (response) {
                if (response.success) {

                    const table = $("#tableRoles").DataTable();

                    $("#deleteRoleModal").modal("hide");
                    table.ajax.reload(null, false);
                    toastr.success("Role deleted successfully!");
                } else {
                    $("#deleteModalMessage").text(response.message).show();
                }
                $("#btnDeleteSpinner").addClass("d-none");
                $("#btnConfirmDeleteRole i").removeClass("d-none");
                $("#btnConfirmDeleteRole").prop("disabled", false);
            })
            .fail(function () {
                $("#deleteModalMessage").text("Error deleting roleid.").show();
            })
            .always(function () {
                $("#btnDeleteSpinner").addClass("d-none");
                $("#btnConfirmDeleteRole i").removeClass("d-none");
                $("#btnConfirmDeleteRole").prop("disabled", false);
                //$("#btnConfirmDeleteRole").removeClass("disabled");
            });
        });

    });

</script>